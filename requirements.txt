# CEREBRO Research Agent — Python Dependencies
# Install with: pip install -r requirements.txt

# ── Web Framework ──────────────────────────────────────────────────────────────
Flask==2.3.3
flask-cors==4.0.0
Werkzeug==2.3.7

# ── Environment ────────────────────────────────────────────────────────────────
python-dotenv==1.0.0

# ── Semantic Retrieval (recommended — install for best retrieval quality) ──────
# sentence-transformers provides all-MiniLM-L6-v2 (80 MB, runs locally, no API key)
sentence-transformers>=2.2.2

# ── Document Parsing ───────────────────────────────────────────────────────────
pypdf>=3.0.0         # PDF support
python-docx>=0.8.11  # DOCX support

# ── LLM Integration (optional — set OPENAI_API_KEY in .env to enable) ─────────
openai>=1.3.0

# ── Testing ────────────────────────────────────────────────────────────────────
pytest>=7.0.0
pytest-cov>=4.0.0

# ── Optional: numpy (installed automatically by sentence-transformers) ──────────
# numpy>=1.24.0
